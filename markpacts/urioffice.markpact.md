# UriPack: urioffice

Thin Markpact — capabilities from [`urioffice/urioffice/manifest.yaml`](urioffice/urioffice/manifest.yaml).
Handlers: existing ``python://`` refs (code in `urioffice/`, not duplicated here).

Generated by `urisys/scripts/generate_pack_markpacts.py`.

```yaml markpact:pack
apiVersion: urisys.io/v1
kind: UriPack
metadata:
  id: urioffice
  version: '1'
  language: python
description: Office document mock/writer/LibreOffice automation for urisys-node.
schemes:
- urioffice
capabilities:
- id: office-status
  uri: urioffice://{host}/query/status
  kind: query
  operation: office.status
  handler: python://urioffice.handlers:status
  side_effects: false
  approval: not_required
- id: office-document-open
  uri: urioffice://{host}/document/command/open
  kind: command
  operation: office.document.open
  handler: python://urioffice.handlers:document_open
  side_effects: true
  approval: required
- id: office-document-save
  uri: urioffice://{host}/document/command/save
  kind: command
  operation: office.document.save
  handler: python://urioffice.handlers:document_save
  side_effects: true
  approval: required
- id: office-document-export_pdf
  uri: urioffice://{host}/document/command/export-pdf
  kind: command
  operation: office.document.export_pdf
  handler: python://urioffice.handlers:document_export_pdf
  side_effects: true
  approval: required
- id: office-writer-render
  uri: urioffice://{host}/writer/command/render
  kind: command
  operation: office.writer.render
  handler: python://urioffice.handlers:writer_render
  side_effects: true
  approval: required
policy:
  default: deny_mutations_without_approval
runtime:
  default_environment: real
  supports:
  - mock
  - local
  - docker
  expose:
  - pack
  - service
  - flow
  - interface
  - adapter
```

```yaml markpact:run
scheme: urioffice
default: flow
modes:
- pack
- service
- flow
- interface
- adapter
service:
  port_hint: 8789
  path: /uri/call
flow:
  ids:
  - urioffice-smoke
uses: []
adapter:
  call: POST /uri/call
  events: GET /events
```

```yaml markpact:tests
tests:
- id: office.status_query
  uri: urioffice://local/query/status
  context:
    approved: true
    dry_run: true
    environment: mock
  expect:
    ok: true
    operation: office.status
- id: office.document.open_command
  uri: urioffice://local/document/command/open
  context:
    approved: true
    dry_run: true
    environment: mock
  expect:
    ok: true
    operation: office.document.open
  payload: {}
```

```yaml markpact:flow id=urioffice-smoke
flow:
  id: urioffice-smoke
  description: Smoke urioffice:// routes (generated from manifest).
defaults:
  approved: true
  dry_run: true
do:
- urioffice://local/query/status
- urioffice://local/document/command/open: {}
- urioffice://local/document/command/save: {}
```

```markdown markpact:docs
## Unpack → `.markpact/`

```bash
cd urioffice
export TELLMESH_ROOT=/home/tom/github/tellmesh
PACK=markpacts/urioffice.markpact.md
urisys markpact run "$PACK" --as flow --approve --dry-run
urisys markpact run "$PACK" --as pack
urisys markpact run "$PACK" --as service --port 8789
urisys markpact run "$PACK" --as interface
urisys markpact run "$PACK" --as adapter
```

- Source manifest: `urioffice/urioffice/manifest.yaml`
- Requires: `pip install -e urioffice` or `TELLMESH_ROOT` for python:// handlers
```
